{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "material-runtime",
  "description": "The render-only E1 material window may receive only native-host scene events; it has no window, shell, file, network, or screen capability.",
  "local": true,
  "windows": ["material"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
