# Eleven v3 prompt revision — 2026-09-17

Candidate; owner listening and native playback review pending. Predecessor: [v2 speech pacing](../week1-voice-pacing-20260917/manifest.md). Working tree based on `83a155f`; no commit/publish. Author: OpenAI Codex/GPT-6 using local source edits, Node/Rust, official ElevenLabs docs and Edge Playwright. Owner planning/research edits retained. No UI/visual or animation edits in this revision; the prior 15-second choreography and manual resize remain.

Owner requested v3, emotional/stacked tags, punctuation-based pacing, longer context for future prompts and separate clips for extreme transitions. They then reported mid-sentence pauses and supplied the exact short example `[speed: 1.25x] [calm] [analytical] Of course. I will pull-up-the-weather in Ithaca.` This later example guides current short-form scripts; it does not demonstrate that v3 guarantees numeric speed or treats hyphens as audio joins.

Changes: shared profile uses `eleven_v3` and stability 0.5; both backends remove stitching fields and v2 controls. All three scripts have front-loaded cues, no ellipses or emphasis capitals. TypeScript and Rust validators enforce allowed tags, stack bounds, SSML rejection, short-form exceptions, maximum length and separate quiet/loud delivery. Future narration guidance is [VOICE_PROMPTING.md](../../VOICE_PROMPTING.md), referenced by AGENTS.md. Current runtime still accepts only three phase IDs, with no arbitrary text/model/voice input. Provider check reuses the production TypeScript payload builder.

Extended narration targets over 250 characters; the owner's compact demo scripts have explicit exceptions. Supported stacked expressions are tested, but arbitrary reactions are not forced into routine updates. Extreme transitions are rejected within a clip and documented for separate generation/crossfaded post-production; no automatic mixing pipeline is claimed. Experimental numeric speed notation is passed literally, with unchanged audio playback rate.

`npm.cmd run build` passed. `npm.cmd test`: **21 passed**. `npm.cmd run rust:test`: **16 passed**. Rust formatting and Clippy passed. Prompt tests cover normal/stacked tags, long narration, required review of short text, malformed tags, unknown tags, SSML, oversized text, profile mismatch and extreme transitions; both backends have corresponding focused cases. Existing request authority, cancellation, concurrency and bounded-response tests pass.

[Live provider results](provider-results.json): all three v3 requests returned HTTP 200 audio using the existing private voice configuration. No v2 fallback. MP3s are 47,273 / 31,808 / 26,793 bytes; temporary audio is outside repository evidence. No keys, voice ID or raw provider diagnostics logged. This confirms the model/payload was accepted, not that every cue was interpreted or internal pauses were eliminated.

Owner retest: restart `npm.cmd run desktop:dev` to clear old speech caches and load the revised backend, request weather and listen for phrase continuity and three-stage pacing. Settings and script changes require listening; native delivery and matching the voice preview are not certified by HTTP or automated playback results. Owner acceptance/date: pending.

Subsequent owner feedback expanded the dialogue to a fixture-grounded weather summary and two wind replies; see [the successor](../week1-wind-dialogue-20260917/manifest.md). This three-script checkpoint is superseded. [Actual MP3 playback results](playback/results.json) and [recording](playback/narration-pacing.webm) passed before that follow-up; evidence remains preserved. No separate source hash snapshot was captured before the follow-up edits.
