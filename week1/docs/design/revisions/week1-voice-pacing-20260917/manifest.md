# week1-voice-pacing-20260917

2026-09-17 — **candidate, owner listening/native review pending**. Predecessor: [resize/voice candidate](../week1-resize-voice-20260917/manifest.md). The earlier biomechanical eye remains the accepted visual direction; this record does not assert a new study-phase acceptance. Working tree based on `83a155f`; [exact changed-source hashes and tool versions](source-hashes.json) accompany this packet. Owner edits to PLANNING.md and the research proposal were preserved. No commit or publication.

Authoring: OpenAI Codex (GPT-6), local source edits, PowerShell/Node, Edge Playwright, Rust tooling and official ElevenLabs documentation. No generated imagery or external design agent. Existing design-engineering guidance applied to the resize affordance. User explicitly requested removal of Celsius options, manual reactive sizing, longer unchanged loading motion and improved speech delivery.

| Before | After | Why |
| --- | --- | --- |
| Celsius source button and memory dialog | Removed visible control/dialog; readings retain units and use validated Markdown preference | Owner wants application-only interaction |
| Small 30 px corner grip | Dithered 42 px target with hover/focus/pressed states | Easier manual resizing; existing pointer capture and keyboard behavior retained |
| 7.5-second folder/CRT staging | Same choreography, 15 seconds total | Space the three voice lines across construction |
| Flash v2.5, isolated sentences, immediate sequential playback | Shared Multilingual v2 profile, neighboring text, scheduled silent intervals | Candidate delivery improvement with EVA retaining speech timing |

Fixtures: checked-in synthetic Ithaca forecast and preference; injected Whisper transcript for wind; synthetic tone and actual ElevenLabs clips for speech tests. Browser captures: Windows, headless installed Edge, 1440×960 CSS px, device scale 1; responsive checks also 1960×530, 960×760, 600×760 and 400×640. Visual noise/blink is time-dependent, without a fixed seed. Root alpha is preserved in PNGs; video uses browser compositing and has no audio track. This is not native desktop transparency evidence.

- [Before weather](before/weather.png), [before resize](before/resized.png), [before checks](before/results.json), [before recording](before/resize-voice.webm).
- [After weather](after/weather.png), [resized/reflowed forecast](after/resized.png), [voice-only wind](after/wind-voice.png), [narrow layout](after/wind-400x640.png), [checks](after/results.json), [recording](after/resize-voice.webm). Reviewed weather/resized stills. Pointer size changed from 1000×638 to 715×478; keyboard adjustment, wind geometry preservation, repeat idempotency, typed wind rejection, reduced motion and four responsive sizes passed with no browser errors.
- [Folder stage](pacing-02/memory.png), [blueprint](pacing-02/raster.png), [reveal](pacing-02/reveal.png), [timing checks](pacing-02/results.json), [motion](pacing-02/narration-pacing.webm). Two immediate-response runs take 15.14 and 15.05 seconds to present. Three clips remain ordered with no overlap; construction starts after 5 seconds, presentation follows readiness, and cancellation during a pause or delayed synthesis prevents late playback. Provider failure keeps the dashboard usable.
- [Live request results](provider-results.json): all three fixed sentences returned HTTP 200 MP3s using the private configured voice. The first attempt from the sandboxed dev server failed with network EACCES; a separately authorized network check succeeded. Keys, voice ID and raw provider diagnostics are absent from evidence.
- [Actual MP3 playback checks](provider-playback/results.json), [recording](provider-playback/narration-pacing.webm): the three live clips were injected into local responses, decoded and played through real Web Audio twice. Clip durations approximately 2.04, 1.44 and 1.49 seconds. Timing/cancellation checks passed. This verifies provider payload acceptance and decoded playback separately, not a fresh native end-to-end run or subjective human likeness.
- The initial timing harness compared speech against the time Playwright noticed readiness, rather than the actual state transition. Its failed assertion is preserved in `pacing/failure.json`; the corrected harness records the transition using a MutationObserver. No application workaround was needed. `live/failure.json` preserves the initial sandbox-blocked attempt.

Checks: `npm.cmd run build` passed; `npm.cmd test` **20 passed**; `npm.cmd run rust:test` **15 passed**; `npm.cmd run rust:clippy` and `npm.cmd run rust:fmt` passed. Browser and Rust implementation both derive the provider profile from `fixtures/narration/voice-profile.json`; context remains generated from the fixed sentence allowlist. New profile/context tests cover both backends. [ElevenLabs research and exact payload](elevenlabs-research.md).

Changed code: App.tsx, Overlay.css, Crt.css, WeatherAssembly.tsx, useNarration.ts, both narration adapters, voice-profile.json, narration-server.test.mjs, narration-pacing-smoke.mjs and opt-in narration-provider-check.mjs. Updated README, DESIGN.md and revision index. Historical artifacts retained.

Owner exercise: restart `npm.cmd run desktop:dev` to load the backend/profile and clear cached speech; request weather, listen to all three sentences over the extended sequence, drag the lower-right corner smaller/larger, then ask for wind using the microphone. Compare the delivery with the chosen voice's preview. Settings are a tested candidate, not a guarantee of identical performance. Native overlay/voice exercise and owner decision/date: **pending**.
