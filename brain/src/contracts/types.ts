/**
 * Hand-written TypeScript types matching brain/schemas/*.schema.json.
 * Stage B1 fills this file; tests assert that type-shaped fixtures pass the
 * generated validators so the two cannot silently drift.
 */
export {};
